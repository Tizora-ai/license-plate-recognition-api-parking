## Reference

Main entrypoints:

- `python.api` — HTTP API
- `python.cli` — local predict / bench / info
- `python.alpr_engine` — shared ONNX engine

API routes:

- `GET /health`
- `POST /recognize`
- `POST /recognize/image`
